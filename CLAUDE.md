# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What echo is

echo is **AI customer-feedback intelligence for e-commerce**: it ingests messy customer feedback from three channels (product **reviews**, support **tickets**, post-purchase **surveys**), classifies each item (category / sentiment / urgency), attaches the **money at stake**, surfaces recurring **themes**, and writes a weekly summary — a prioritized, dollar-weighted action list.

**`README.md` is the authoritative design spec — read it first.** It defines the taxonomy→owner→money table, the money-engine formulas, urgency anchors, the weekly-summary contract, and the endpoint list. `CORPUS.md` documents the dataset build.

**Target shape:** exactly **2 app containers** — `backend` (FastAPI + the whole pipeline) and `frontend` (Streamlit, a thin client that talks only to the API) — plus **PostgreSQL with the pgvector extension** as an external data store (vectors live in Postgres, *not* Milvus).

## Current state (keep this section honest as you build)

- **Built:** the offline **corpus builder** (`src/echo/corpus/`), **Postgres persistence** (`src/echo/db/`), the **classify** stage (`src/echo/classify/`), the **money engine** (`src/echo/money/`), and the **embeddings** stage (`src/echo/embed/` + `src/echo/db/vector.py`). A 15,000-item corpus is loaded; all 15,000 items are classified (`analysis` full, `gpt-4o-mini`/`classify_v1`) and all 14,765 texted items are embedded (`embeddings` table, `text-embedding-3-small`, 1536-d, HNSW cosine index).
- **Money engine** is pure SQL/Python (no table, no LLM): `python -m echo.money [--week|--demo-week]` reports per-category Direct Exposure (deterministic) + modeled Retention Risk (low/base/high) + coverage tier (currently T3). `money.engine.summary()` / `urgent_items()` are reused downstream (summary, and the future `/urgent` endpoint).
- **pgvector is installed** on the local `postgresql@16` (built from source against pg16 — the brew bottle only shipped pg17/18 files). `db/vector.py::create_all()` runs `ensure_extension()` before DDL; `embed.nearest()` does cosine top-k.
- **Themes** (`src/echo/themes/`, `python -m echo.themes [--week|--trend N|--threshold]`): per week, agglomerative cosine clustering (threshold 0.40) over embeddings of actionable items (negative+neutral), keep clusters ≥3, rank top-10 by revenue-at-risk (`money.engine.exposure_for_items`), LLM writes a specific `<component>: <issue>` label with a generic-label validator (retry ×2), owner = majority category→team. Writes `themes` + `theme_members` idempotently (a re-run replaces the week). Populated for `DEMO_WEEK` + 4 preceding weeks (33 themes). Clustering/ranking are $0/SQL; only labelling costs tokens (~$0.002 for 5 weeks). **`item_count` is the whole cluster's size, not a count of items matching whatever specific detail the label mentions** — the labeller only ever sees 6 representative quotes, not all cluster members, so `THEME_PROMPT_VERSION` bumped to `theme_v2` after catching it pull a specific number (e.g. "delayed by 14 days") from a single example quote into a label covering 144 items with wildly different delay lengths (2–46 days). The prompt now explicitly forbids stating a number unless it's the shared figure across most shown quotes; re-ran and confirmed the fix (that theme now reads "Delivery: shipments arrive late", same 144 items, same $10,258).
- **Weekly summary** (`src/echo/summary/`, `python -m echo.summary [--week]` or `POST /summary/weekly`): SQL computes every number (volume + sentiment WoW, top drivers by revenue-at-risk, urgent queue); the LLM only narrates + picks a driver-category per action; echo injects the `$` (authoritative per-category exposure) and owner. Writes one upserted row per week to `weekly_summary` (+ `llm_calls` audit). Uses real themes when present (`source: theme`), else falls back to categories-by-revenue-at-risk (`source: category_fallback`). `runner.run()` accepts an optional `engine=` (reused by the API instead of opening a throwaway connection pool per call, matching every other reused pipeline function's convention).
- **API** (`src/echo/api/`, `python -m echo.api` or `uvicorn echo.api.main:app`): FastAPI, SQLAlchemy Core + parameterized SQL, no ORM session. Endpoints: `/health` (db+llm+build_id), `/stats/{overview,volume,sentiment,crosstab}`, `/themes`, `/urgent`, `GET /summary/weekly` (read back a previously generated week), `POST /summary/weekly` (generate live for a given week — gated on `/health.llm` — then store; the primary path, not a passive read), `GET /feedback` (filter+paginate), `POST /feedback` (live classify+embed+money, gated on `/health.llm`; accepts optional `order_value`/`refund_amount`/`fulfillment_outcome` so the money engine has real fields to compute from — without them a live item has no order attached and legitimately shows near-$0 exposure; `language` is real per-item detection via `langdetect` — `en`/`pt`/etc., `None` on genuine detection failure rather than a hardcoded guess — since unlike the batch corpus, which is Portuguese by construction, a live submission can be any language), `POST /ask` (RAG, gated on `/health.llm`), `GET /eval/gold` (model-evaluation report card). Reuses `classify.classify_text()` + `embed.embed_texts()`. Every figure is SQL-computed. All endpoints verified against the live DB.
- **RAG / "ask echo"** (`src/echo/rag/`, `python -m echo.rag "question"` or `POST /ask`): embed the question (`embed.embed_texts`) → pgvector cosine top-k over `embeddings` (`rag/retrieve.py`, query vector bound as a `CAST(:qvec AS vector)` literal — no stored row on the query side) → LLM writes a grounded answer + a list of `item_id`s it drew on (`rag/prompts.py::RagAnswer`, hallucinated ids dropped) → echo appends a stats block computed purely in SQL over the same retrieved set (sentiment split, top category, `money.exposure_for_items` dollar figures) — the model never emits a number. One `llm_calls` audit row per call (`call_type='rag'`). Verified live: "late deliveries?" and a billing-charges question both returned grounded, cited, SQL-numbered answers via both the CLI and `POST /ask`.
- **Frontend** (`src/echo/frontend/`, `python -m echo.frontend` or `streamlit run src/echo/frontend/app.py`; base URL from `ECHO_API_URL`, default `http://localhost:8000`): a genuinely thin Streamlit client (every number/answer comes from `api_client.py` calling the FastAPI backend, nothing computed locally). **As of 2026-07-29 it is a single-router app** (`app.py` drives `st.navigation`/`st.Page`), not the old auto-`pages/` multipage layout — see the Authentication bullet below for the router, landing screen, role-based nav, and the no-emoji/no-em-dash house style. Content views live in **`views/`**: `overview.py` (KPI tiles, money-at-stake tiles, volume by category/source, sentiment split + weekly trend, category-by-source heatmap), `urgent_queue.py`, `themes.py` (chart + expandable cards, always top 10 = `config.TOP_THEMES`; the old slider was removed), `weekly_summary.py` (pick a week → **Generate** → live `POST /summary/weekly`, gated on `/health.llm`), `live_feedback.py` (`POST /feedback`, gated, with an "Order details" section so the money engine has fields to compute from; gen_pop-only), `ask_echo.py` (`POST /ask`, gated), `model_evaluation.py` (`GET /eval/gold`). `charts.py` builds Plotly figures per the dataviz-skill method (one hue for magnitude bars/heatmap, reserved good/neutral/critical status colors for the 3-way sentiment split/trend, titles+axes+legends+automargin on every figure); `common.py` holds the landing/login screen, role-aware sidebar/logout, and a compact-currency (`R$115k`) money-metric row. Verified live end-to-end with a real backend (Playwright/Chromium earlier; headless `AppTest` for the auth/role flows).
- **Docker packaging** (`docker-compose.yml`, `docker/{backend,frontend}.Dockerfile`, `docker/seed/`): the target 2-app-container shape is real — `docker compose up --build` → `db` (`pgvector/pgvector:pg16`, named volume `pgdata`) → `backend` (`uvicorn`-only entrypoint; the corpus loader must never run against a seeded DB) → `frontend`. First boot on an empty volume auto-restores `docker/seed/00_extensions.sql` (`CREATE EXTENSION vector`) + `docker/seed/10_echo_seed.sql.gz` (a `pg_dump --exclude-table-data=embeddings` of the live DB — full schema + all data *except* embeddings rows, kept out because RAG needs an `OPENAI_API_KEY` regardless, so shipping them for free buys nothing; `python -m echo.embed` inside the backend container backfills them once a key is set). `docker compose down -v` wipes the volume to re-seed from scratch. An opt-in `seed` service (`--profile rebuild`) regenerates the dump from a running `db`. `api/main.py` gained a `lifespan` startup hook (`vector.create_all` then `schema.metadata.create_all`, in that order — the extension must exist before the unscoped create_all, since importing `vector` registers the `embeddings` table on the same shared metadata) as a safety net so the backend is correct even without the seed. **Verified live**: built both images, brought up all 3 containers on a fresh volume, confirmed the seed restored (15,003 feedback/analysis rows, 0 embeddings rows as designed), screenshotted the dashboard working end-to-end through the container network, and explicitly tested the no-`OPENAI_API_KEY` path (`/health` → `llm:false`, frontend shows a clean gating banner, no crash) before restoring the key and confirming recovery. Skipped Git LFS for the 18MB seed file (well under GitHub's 100MB hard limit) to avoid a global git-config change (`git lfs install`) the safety protocol disallows without the user opting in.
- **Gold set** (`gold_candidates` table, `set_name='target'`, 40 rows): labeled — `gold_category`/`gold_sentiment`/`gold_urgency` filled in by reading each item's Portuguese text directly against `classify/prompts.py`'s own taxonomy + urgency rubric, then **cross-verified by the user**, who updated `labeler_id` to `'human'` on every row once satisfied. These are now genuine human-verified ground truth, not a caveated AI-assisted approximation. Several rows carry `labeler_notes` flagging ambiguous calls and cases where a `messy_flags` tag (e.g. `sarcasm`, `spam`) didn't match the actual text content.
- **Evaluation** (`src/echo/classify/evaluate.py`, `python -m echo.classify.evaluate` or `GET /eval/gold`): two report cards, pure SQL/Python (no LLM, no new table). `gold_report()` — category confusion matrix (10×10) + sentiment confusion matrix (3×3) + urgency MAE/exact-match/within-±1, against the 40 human-verified gold items; also returns a sorted `mismatches` list (worst urgency-error first) for drill-down. `silver_sentiment_report()` — LLM sentiment vs. the star/NPS-derived silver label across every classified item with a usable score (thousands, reusing the `analysis.source_score_disagreement` flag `classify/crosscheck.py` already computes). Live numbers: **90.0% category accuracy, 87.5% sentiment accuracy, urgency MAE 0.25 (97.5% within ±1)** on the gold set; **90.3% silver-sentiment accuracy across 8,548 scored items**. Wired into a new **Model Evaluation** frontend page (`pages/6_Model_Evaluation.py`) — KPI tiles, both confusion matrices as tables (10 categories exceeds the dataviz-skill's ~7-class comfort threshold for color-only encoding, so real numbers in a table, not just a heatmap), and expandable mismatch cards with the actual text + gold-vs-predicted + labeler note. One real finding it surfaced: the classifier over-trusts a customer's own "é super urgente!" wording even when the order already arrived fine (gold urgency 2, predicted 4) — a genuine calibration gap, not just a demo number.

**Gotcha (bit us twice, 2026-07-28): local Postgres + the Docker `db` container both default to host port 5432 — whichever grabbed it first wins, silently** (same for 8000/8501 vs a local `python -m echo.api` / `streamlit run`). It's not just "whichever's up wins" either — Docker Desktop's macOS port-forwarding proxy doesn't always collide with a plain local bind the way you'd expect, so local Postgres and the Docker container have been caught genuinely listening on 5432 *at the same time*. Worse: they're not just competing for the same port, they're two entirely separate databases — the Docker backend talks to its own container's Postgres over the internal Docker network (`db:5432`), completely isolated from the local one, so data submitted through whichever app you're *not* looking at silently goes nowhere you'd think to check.

**Resolved convention: local is the primary, day-to-day instance. Docker is only for periodically verifying the packaged deployment still works — bring it up, check it, then `docker compose down` again; never leave it running alongside local dev.** Before starting either, run **`./scripts/status.sh`** to confirm nothing's already bound (it reports `local` / `docker` / a genuine `conflict` / `free` per port). When you do need to verify Docker packaging, use **`./scripts/refresh-docker.sh`** (regenerates the seed from local Postgres, rebuilds, `down -v`, `up -d` — refuses to run unless port 5432 is unambiguously local Postgres first, so it can't dump stale Docker data back into the seed), check what you need to check, then tear it back down.
- **CI image publishing** (`.github/workflows/publish-images.yml`): on push to `main` touching `src/**`, `docker/**`, or `pyproject.toml` (or manually via `workflow_dispatch`), builds both `docker/backend.Dockerfile` and `docker/frontend.Dockerfile` and pushes to GHCR as `ghcr.io/<owner>/echo-backend` / `echo-frontend`, tagged `:latest` + `:<commit-sha>`. Path-filtered deliberately — a docs/memory-only commit shouldn't trigger a rebuild. Uses the repo's built-in `GITHUB_TOKEN` (no PAT needed); GHCR packages default to **private** on first push regardless of repo visibility — one manual step (Package settings → visibility) is needed if they should be public. This publishes an archived/distributable image; local dev still uses `docker compose up --build` (builds locally, unrelated to this workflow).
- - **Authentication + role-based access** (`src/echo/auth/`, wired into `src/echo/api/`): JWT bearer auth over one `users` table with two roles — **GEN-POP** (submit feedback + view only their own) and **COMPANY** (all feedback + analytics). `auth/security.py` = pure bcrypt hashing + PyJWT HS256 encode/decode (no DB/web imports, reused by the CLI); `auth/service.py` = Core user store (`create_user`/`get_by_email`/`get_by_id`/`authenticate`, unique-email → clean `EmailExistsError`); `python -m echo.auth` CLI = `seed` (demo company admin + gen_pop from `SEED_*` env, idempotent), `create-user`, `list`. API side: `api/routers/auth.py` = `POST /auth/register` (public sign-up — **always** gen_pop, never company), `POST /auth/login` (OAuth2 password form so Swagger shows an **Authorize** padlock), `GET /auth/me`; `api/deps.py` gained the project's first `Depends()` — `get_current_user` (401 on bad/expired token or inactive user) + `require_company` (403 otherwise). Guard wiring in `main.py`: health + auth are public; `stats`/`themes`/`urgent`/`summary`/`ask`/`eval` are attached with `dependencies=[Depends(require_company)]` (server-side 403 — a gen_pop can't bypass it via Swagger, since Swagger is just an HTTP client); `feedback` has per-endpoint guards (POST stamps `submitter_id`; GET filters to own for gen_pop, all for company). `feedback.submitter_id` (FK→`users.id`, NULL for the 15k batch corpus) powers "view own"; it's added to pre-existing DBs by an idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` in the `lifespan` hook (create_all never alters an extant table). Frontend (**UX refined 2026-07-29**): `app.py` is a single **router entrypoint** driving `st.navigation`/`st.Page` (Streamlit 1.60) — the old auto-discovered `pages/` folder was renamed to **`views/`** (snake_case, de-emojified) so it can be role-filtered. Not logged in → `common.render_landing()`: a glassy-black screen with a single centered `echo` wordmark, CSS arc waves on either side, and a **Login** button that reveals login/create-account tabs. Logged in: **gen_pop** gets a single `views/live_feedback.py` page (form + their own submissions) with the sidebar hidden and a top-right logout, **no health/status**; **company** gets the six analytics pages in the sidebar (Overview default, Urgent Queue, Themes, Weekly Summary, Ask echo, Model Evaluation — **not** Feed a feedback) plus a plain-text account+health box. Views are pure content (no `set_page_config`/guards); LLM-gated views read `api_client.health()` directly. `api_client.py` attaches the session bearer token to every call and leaves the per-user "view own" list **uncached** (`st.cache_data` is process-global). **House style: no emojis, no em dashes in the UI** (see the [[feedback_no_emoji_em_dash_ui]] memory). Themes has no slider (always top 10 = `config.TOP_THEMES`). New deps in the `app` extra: `pyjwt`, `bcrypt`, `python-multipart`. **`JWT_SECRET` defaults to an insecure dev value and warns at import** — set it in `.env` for anything real. Verified end-to-end: full curl authz matrix (gen_pop → 403 on every analytics endpoint, sees 0 own vs company's 15,013; a real gen_pop submission classified + visible only to them; unauth → 401; duplicate register → 409; bad token → 401), the OpenAPI security scheme + `/docs` Authorize flow, and 17/17 headless Streamlit `AppTest` role-gating checks.
- **Sign-up hardening + User Analytics dashboard (2026-07-29, same day):** three more additions on top of the auth work above. (1) **Password policy**: `RegisterIn` (`api/schemas.py`) gained a `field_validator` requiring 8+ chars, upper, lower, digit, special char — a bad password gets one `422` naming everything missing (Pydantic v2 prefixes custom validator messages with "Value error, ", stripped in the frontend before display). Policy is public-registration-only; `python -m echo.auth create-user`/`seed` are untouched. (2) **Register no longer auto-logs-in**: `POST /auth/register` now returns `UserOut` (201, no token); the frontend sign-up form gained a **Confirm password** field (client-side mismatch check) and was refactored from `st.tabs` to an `st.radio` mode toggle (`common.py`, key `auth_mode`) — tabs can't be switched programmatically, but a successful sign-up needs to land back on the login form with a one-time "Account created" banner (`st.session_state["signup_success"]`). Gotcha hit and fixed: Streamlit forbids writing a widget's `session_state[key]` after that widget is instantiated in the same run, so the mode-switch is deferred through a plain flag (`_force_login_mode`) checked *before* the radio widget is created on the next rerun. (3) **User Analytics** (`GET /users/analytics`, COMPANY-only, new `api/routers/users.py`): lists every registered user (`LEFT JOIN` so zero-submission accounts still appear) with per-user sentiment split, net-sentiment "overall opinion", avg urgency, category focus, weekly sentiment-over-time, and money at stake — reusing `money.engine.exposure_for_items` per user, same as `feedback.py`. Scoped to `feedback.submitter_id IS NOT NULL` only (the 15k batch corpus is invisible here). New `views/user_analytics.py`: a leaderboard table (`pandas`/`st.dataframe`) + a `st.selectbox` drill-down into one user's charts (reusing `charts.sentiment_split_bar`/`magnitude_bar`/`sentiment_trend` and `common.money_metrics`) and their submission history *with* classification labels shown (an admin view — unlike the customer-facing Feed a feedback page, which deliberately hides them). Added to the company sidebar nav in `app.py`. One real bug caught by testing: the leaderboard's "Avg urgency" column mixed floats and the string `"-"` for no-data rows, which pyarrow couldn't serialize cleanly (Streamlit auto-recovered with a warning) — fixed by formatting the whole column as strings. Verified: full curl matrix against both local and a rebuilt Docker image (weak/strong password, register-then-login proving the account was really created, `/users/analytics` 401/403/200 with real submitted data), plus 17 headless `AppTest` checks (confirm-password mismatch, weak-password rejection, successful signup returning to login with the banner and no auto-login, existing login mode unaffected, and the new page rendering leaderboard + drill-down without exceptions).

**Not yet built:** none — every MVP stage plus the optional gold-set evaluator and JWT auth are built and verified end-to-end.

## Setup & commands

**`make help`** lists every command below as a `make` target (`make install`, `make run-api`, `make dev`, `make docker-refresh`, `make themes WEEK=...`, etc.) — same commands, shorter to type. The raw commands still work identically; the `Makefile` is a thin wrapper, nothing more.

Python **3.13** venv at `.venv`. The `echo` package is **not** installed editable — either prefix runs with `PYTHONPATH=src`, or `pip install -e .`.

```bash
python3.13 -m venv .venv
.venv/bin/pip install -e ".[corpus,db,pipeline,app,frontend,dev]"   # extras: corpus, db, pipeline, app, frontend, dev
cp .env.example .env                             # set OPENAI_API_KEY (only needed for LLM stages)

# Run a stage (every stage is a package with a __main__):
PYTHONPATH=src .venv/bin/python -m echo.corpus              # full corpus build + verify
PYTHONPATH=src .venv/bin/python -m echo.corpus --limit 50   # tiny dry-run per source
PYTHONPATH=src .venv/bin/python -m echo.corpus --offline    # deterministic stub, no API calls
PYTHONPATH=src .venv/bin/python -m echo.corpus --stage econ|verify|all   # single stage
PYTHONPATH=src .venv/bin/python -m echo.db                  # (re)load data/processed -> Postgres
PYTHONPATH=src .venv/bin/python -m echo.db --keep           # append instead of truncate+reload
PYTHONPATH=src .venv/bin/python -m echo.auth seed           # create demo login accounts (company admin + gen_pop)
PYTHONPATH=src .venv/bin/python -m echo.auth create-user --email x@y.com --password pw --role company  # one account
PYTHONPATH=src .venv/bin/python -m echo.api                 # run the backend (uvicorn, :8000)
ECHO_API_URL=http://localhost:8000 PYTHONPATH=src .venv/bin/python -m echo.frontend   # run the dashboard (:8501)
# ^ this (local) is the day-to-day way to run echo — see the port-collision gotcha below.

# Docker is for periodically verifying the packaged deployment, not daily use — bring it up,
# check it, then `docker compose down` again rather than leaving it running alongside local dev:
docker compose up --build                        # -> http://localhost:8501 (needs Docker Desktop running)
docker compose exec backend python -m echo.auth seed   # seed login accounts (the DB seed dump has no users)
docker compose down -v                           # wipe the volume (forces a re-seed on next `up`)
docker compose --profile rebuild run --rm seed   # regenerate docker/seed/10_echo_seed.sql.gz from the running db

./scripts/status.sh           # which instance (local vs Docker) is answering on 5432/8000/8501 right now
./scripts/refresh-docker.sh   # regenerate the seed from local Postgres, then rebuild + fully reset the Docker stack

.venv/bin/ruff check src/                        # lint (config in pyproject; must be clean)
psql -h localhost -d echo                        # DB console
```

Long/expensive runs (full LLM builds) should go to the background with logs to a file — `tail` them, don't dump them into context.

## Architecture — the load-bearing ideas (read multiple files to grasp)

1. **Anti-hallucination invariant (non-negotiable).** The LLM only *classifies* and *writes prose*; **every number and dollar figure is computed in SQL/Python and injected** into the model. Never let an LLM emit a figure. `order_economics` is the single money source of truth (all $/dates originate there).

2. **`config.py` is the single source of truth for every knob** — `SEED`, the fixed **10-category taxonomy** (`CATEGORIES`), corpus sizes, synthesis mixes, MCMC/cluster thresholds, prompt-version strings, model name, and `DATABASE_URL`. New assumptions go here, never inline.

3. **The common envelope.** `schemas/envelope.py::CorpusItem` is the one shape every feedback item normalizes to; its validators *enforce* invariants (tickets carry no score, reviews/surveys always do; `created_at` inside the Olist window; category ∈ the fixed 10). Reused by the corpus builder, the loader, and every future stage.

4. **Data substrate.** All data lives under `data/` (gitignored) in `raw/` (9 Olist CSVs), `processed/` (build outputs: `corpus.jsonl`, `order_economics.parquet`, gold set, manifest), `interim/` (the run-once LLM disk cache). Postgres db `echo` holds 8 tables (`db/schema.py`, SQLAlchemy Core, one `metadata`): `order_economics` (one row per order — the money/fulfillment backbone), `feedback` (the 15k items; provenance + messy-flags as JSONB), `gold_candidates`, and the pipeline tables `analysis` (versioned classify output), `llm_calls` (audit), `themes` + `theme_members`, `weekly_summary`.

5. **Reuse these patterns; don't reinvent.**
   - `corpus/synth_common.py` is the template for **any** LLM stage: `ChatOpenAI(...).with_structured_output(PydanticModel)` + tenacity retry + a disk cache keyed by content hash + a `RecordingGenerator`→`warm_cache` two-phase pattern for concurrent batch throughput.
   - `db/load.py` has the SQLAlchemy Core bulk-insert / batching / `_nonul` (strip NUL bytes Postgres rejects) patterns for any DB writer. The loader does `create_all` (never drops) + `TRUNCATE`s **only** the corpus tables — so it never wipes populated pipeline tables.

6. **Reproducibility.** One `SEED` drives all sampling; `item_id` is a `uuid5` (idempotent re-runs); LLM outputs are cached to disk (run-once) and, for analysis, to a table **versioned by `model_name` + `prompt_version`** (bump the prompt version to re-run without mutating prior results). `corpus/verify.py` runs 14 invariant checks and gates the build.

## When I ask you to "explain" something (explanation mode)

Any time my prompt is an "explain this" request — the project, a stage, a decision, a chunk of code, an error, whatever — **default to explaining it to a smart person with zero technical context**, someone who knows only "we're building an AI agent to do something." Do this *unless* I explicitly say "explain technically" / "for engineers." Rules:

- **Start from zero.** Assume no knowledge of the codebase, the stack, or the jargon. Don't reference file names, table names, or library names as if they're understood.
- **Translate every unavoidable technical term inline**, in plain words, the first time it appears (e.g. "a database — a well-labeled digital filing cabinet").
- **Lead with the *why*, not the *how*.** What real-world problem does this solve for a real person (a store owner, a CX manager)? Use concrete analogies and the actual customer-feedback scenario, not abstractions.
- **Follow a shape:** what it is → why it matters → where we are (built vs. still-to-come, honestly) → a one-line "if you remember one thing" summary at the end.
- **Stay truthful and grounded** — same anti-hallucination discipline as the product: real numbers, honest progress, no glossing over what's unfinished. Simplify, don't fabricate.
- **Offer, at the end, to turn it into a one-page visual** they could show in a meeting.

Keep it warm and readable — narrative prose over dense bullet-walls, minimal formatting.

## Conventions

- **src-layout**; each stage is a package under `src/echo/` exposing a `__main__.py`, run as `python -m echo.<stage>`.
- Every module opens with a **plain-English + light-technical docstring** — match that style.
- **New LLM stage:** add its prompt-version + knobs to `config.py`; write to a versioned table; cache by content hash; mirror `synth_common.py`.
- **New table:** add to `db/schema.py` (single `metadata`); decide whether the loader should truncate it.
- **The dataset is real-grounded** (that's what makes demos credible): reviews are genuine Brazilian-Portuguese Olist reviews; tickets + surveys are LLM-synthesized but grounded on real orders and flagged `synthetic`. Text is Portuguese, analyzed **in place** (the LLM reads PT, emits English labels).
